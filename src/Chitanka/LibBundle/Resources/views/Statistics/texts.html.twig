<table class="table table-condensed table-striped">
	<caption>Произведения по форма</caption>
	{% for type, count in text_types %}
		<tr>
			<td><a href="{{ path('texts_by_type', {'type': type}) }}">{{ ('text.type.singular.'~type) | trans }}</a></td>
			<td>{{ count }}</td>
		</tr>
	{% endfor %}
</table>
