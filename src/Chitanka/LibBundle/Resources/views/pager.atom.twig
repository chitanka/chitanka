{% if pager.show %}
	{% if pager.has_prev %}
		<link rel="prev" type="application/atom+xml" href="{{ path(route, route_params|merge({'page': pager.prev, '_format': 'atom'})) }}" title="Предишна страница"/>
	{% endif %}
	{% if pager.has_next %}
		<link rel="next" type="application/atom+xml" href="{{ path(route, route_params|merge({'page': pager.next, '_format': 'atom'})) }}" title="Следваща страница"/>
	{% endif %}
{% endif %}
