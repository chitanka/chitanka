# Phrases used in \App\Validator\Constraints\NotSpam
# If the input text matches one of these phrases as a regular expression, then it is labeled as spam.
lottery|inheritance
big.*profit
this is a #stop phrase