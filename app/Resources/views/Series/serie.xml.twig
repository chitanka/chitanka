<id>{{ serie.id }}</id>
<slug>{{ serie.slug }}</slug>
<name>{{ serie.name }}</name>
{% if serie.origName is defined %}<orig-name>{{ serie.origName }}</orig-name>{% endif %}
{% if serie.authors is defined %}
	{% for person in serie.authors %}
		<author>
			{% include 'App:Person:person.xml.twig' %}
		</author>
	{% endfor %}
{% endif %}
