{% if book.sequence %}
	{% import 'helper.html.twig' as helper %}
	{% import 'link.html.twig' as link %}
	{{ helper.printBreadcrumb({
		(path('sequences')): 'Поредици',
		'': link.sequence(book.sequence)
	}) }}
{% endif %}
