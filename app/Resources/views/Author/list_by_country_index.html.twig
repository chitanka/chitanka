{% extends 'layout.html.twig' %}

{% set title = ('authors.by.country.'~by)|trans %}
{% block content %}

<ul>
	{% for country in countries %}
		<li><a href="{{ path('authors_by_country', {'country': country.code, 'by': by}) }}">{{ country.name }}</a></li>
	{% endfor %}
</ul>

{% endblock %}
