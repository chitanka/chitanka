{% extends 'App::layout.html.twig' %}

{% set title = 'authors'|trans %}
{% block head %}
	{{ parent() }}
	<link rel="search" type="application/opensearchdescription+xml" href="{{ path('authors_search', {'_format': 'osd'}) }}" title="Търсене по автори в Моята библиотека" />
	<link rel="search" type="application/opensearchdescription+xml" href="{{ path('persons_search', {'_format': 'osd'}) }}" title="Търсене по личности в Моята библиотека" />
{% endblock %}
{% block content %}
	{% include 'App:Author:navigation.html.twig' with {'by': '', 'letter': ''} %}
	<h2>Държава</h2>
	<ul>
		{% for country, count in countries %}
			<li>
				<a href="{{ path('authors_by_country', {'country': country}) }}">{{ ('country.'~country) | trans }}</a>
				<span class="badge">{{ count }}</span>
			</li>
		{% endfor %}
	</ul>
{% endblock %}
