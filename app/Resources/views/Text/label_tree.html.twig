<ul class="two-columns">
{% for label in labels %}
	<li>
		<a href="{{ path('texts_by_label', {'slug': label.slug}) }}">{{ label.name }}</a>
		({{ label.nrOfTexts }})
	{% if label.children is defined %}
		{% include 'App:Text:label_tree.html.twig' with {'labels': label.children} %}
	{% endif %}
	</li>
{% endfor %}
</ul>
