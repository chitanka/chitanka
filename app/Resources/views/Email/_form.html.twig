<p>Чрез долния формуляр можете да пратите писмо на потребител по електронната поща. Адресът, записан в <a href="{{ path('user_edit', {username: sender.username}) }}">настройките ви</a>, ще се появи в полето „От“ на изпратеното писмо, така че получателят ще е в състояние да ви отговори.</p>

{{ form_start(form, { attr: {class: "form-horizontal"} }) }}

	{{ form_errors(form) }}

	<div class="form-group">
		<label class="col-sm-2 control-label">От:</label>
		<div class="col-sm-10">
			<div class="form-control">
				<a href="{{ path('user_show', {username: sender.username}) }}">{{ sender.username }}</a>
			</div>
		</div>
	</div>
	<div class="form-group">
		<label class="col-sm-2 control-label">До:</label>
		<div class="col-sm-10">
			<div class="form-control">
				<a href="{{ path('user_show', {username: recipient.username}) }}">{{ recipient.username }}</a>
			</div>
		</div>
	</div>
	<div class="form-group">
		{{ form_label(form.subject, 'Относно:', { label_attr: {class: 'col-sm-2 control-label'} }) }}
		<div class="col-sm-10">
			{{ form_errors(form.subject) }}
			{{ form_widget(form.subject, { attr: {class: 'form-control', autofocus: 'autofocus'} }) }}
		</div>
	</div>
	<div class="form-group">
		{{ form_label(form.message, 'Съобщение:', { label_attr: {class: 'col-sm-2 control-label'} }) }}
		<div class="col-sm-10">
			{{ form_errors(form.message) }}
			{{ form_widget(form.message, { attr: {class: 'form-control'} }) }}
		</div>
	</div>
	<div class="form-submit">
		{{ form_widget(form.save, { attr: {class: 'btn btn-primary'} , label: 'Изпращане на писмото' }) }}
	</div>

{{ form_end(form) }}
