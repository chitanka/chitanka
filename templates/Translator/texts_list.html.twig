{% import 'text.html.twig' as text_lister %}
	<h3>
		{% if series.slug is defined %}
			<a href="{{ path('series_show', {'slug': series.slug}) }}">{{ series.name }}</a>
		{% else %}
			{{ series.name }}
		{% endif %}
	</h3>
	{{ text_lister.text_list_with_author(texts, {'download_links': true}) }}
