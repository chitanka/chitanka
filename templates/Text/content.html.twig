<div id="textstart{{ id_suffix|default('') }}" class="text-content text-content-main content-type-{{ text.type.code }}" data-id="{{ text.id }}">
	{{ text.contentHtml(asset('/'), part, obj_count, id_suffix is defined ? ('p'~id_suffix) : '')|raw }}
</div>
