<categories>
{% for category in categories %}
	<category>
		<id>{{ category.id }}</id>
		<name>{{ category.name }}</name>
		<slug>{{ category.slug }}</slug>
		<nr-of-books>{{ category.nrOfBooks }}</nr-of-books>
	</category>
{% endfor %}
</categories>
