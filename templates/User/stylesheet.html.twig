{% if stylesheet %}
	<link rel="stylesheet" href="{{ asset(stylesheet) }}" id="activeStyleSheet">
{% endif %}
{% for stylesheet in extra_stylesheets %}
	<link rel="stylesheet" href="{{ asset(stylesheet) }}">
{% endfor %}
<script>var user_functions = [];</script>
{% for javascript in extra_javascripts %}
	<script src="{{ asset(javascript) }}"></script>
{% endfor %}
